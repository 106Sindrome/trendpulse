{
  "name": "trendpulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrendPulse — Bloomberg Terminal for viral content. Real-time dashboard aggregating trending content across the internet.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  }
}
