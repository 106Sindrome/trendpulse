services:
  trendpulse:
    build: .
    container_name: trendpulse
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # persists custom watchlist entries
    restart: unless-stopped
